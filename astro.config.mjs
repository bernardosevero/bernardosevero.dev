import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://bernardosevero.github.io',
  base: '/bernardosevero.dev',
  output: 'static',
  devToolbar: { enabled: false },
});
